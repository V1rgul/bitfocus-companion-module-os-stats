# companion-module-os-stats

See [HELP.md](./companion/HELP.md)

## Getting started

Run `yarn` to install dependencies.

Build once with `yarn build` — this produces `dist/main.js` and is enough for Companion to load the module.

While developing, `yarn dev` runs the TypeScript compiler in watch mode.

Package for distribution with `yarn dist`.
